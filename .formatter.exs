# Used by "mix format"
[
  inputs: ["mix.exs", "{config,lib,test}/**/*.{ex,exs}"],
  locals_without_parens: [
    assert_dispatched: 3,
    assert_dispatched: 4,
    assert_dispatch: 4,
    on_exit: 1
  ]
]
